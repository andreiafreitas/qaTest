# Upsun configuration, generated using AI at: 2026-04-13T15:49:04.115Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  qatests:
    type: nodejs:24
    build:
      flavor: none
    hooks:
      build: |
        set -ex
        npm install --omit=dev
    web:
      commands:
        start: npm run cy:run

routes:
  https://{default}/:
    type: upstream
    upstream: qatests:http